<!DOCTYPE html>
<head>
    <title>Account Statement</title>
</head>
<body>
    <%- include('./subviews/navbar')-%>
    <%- include('./subviews/sideNavbar')-%>
    <div class="main_content">
        <div class="header ps-3 pt-3">
            <h6>Account Statement: A/c no.&nbsp;<%= user[0].accountNo%></h6>
        </div>
        <div class="info">
            <div class="card p-4" style="width:100%">
                <table class="table table-hover" >
                    <thead>
                        <tr>
                            <th>Txn No.</th>
                            <th>Date</th>
                            <th>Statements</th>
                            <th style="text-align:center">Debit/Credit</th>
                            <th style="text-align:center">Balance</th>
                        </tr>
                    </thead>
                    <tbody></tbody>
                        <% for(var i=0; i<user.length; i++) { %>
                        <tr>
                            <td><%= i+1 %></td>
                            <td><%= user[i].Date.toISOString().slice(0,10) %></td>
                            <td><%= user[i].stats %></td>
                            <td style="text-align:right; padding-right:60px"><%= user[i].amount %></td>
                            <td style="text-align:right; padding-right:60px"><%= user[i].balance%></td>
                        </tr>
                        <% } %>
                    </tbody>
                </table>
                <a href="/admin/listStats" class="btn btn-primary" style="width: 100px" >Go Back</a>
            </div>
        </div>
    </div>
</body>
